# cv-fail-detect
I will build a computer vision system that automatically detects camera failures by analyzing the video feed itself. Think of it as a health monitor for infrastructure that normally operates blind.
